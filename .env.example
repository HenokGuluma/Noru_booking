# Copy to .env.local. Never commit the real thing.

NODE_ENV=development

DATABASE_URL=postgres://noru:noru_dev_only@localhost:5434/noru_crew
DATABASE_POOL_MAX=10

# Session cookie signing secret. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
SESSION_SECRET=replace-me-with-a-real-secret

OBJECT_STORE_ENDPOINT=http://localhost:9000
OBJECT_STORE_BUCKET=noru-crew

OPERATING_TIMEZONE=Africa/Addis_Ababa
DEFAULT_LOCALE=en-ET
